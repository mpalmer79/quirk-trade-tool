{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    // Do NOT include Vitest types when compiling for production.
    "types": ["node"]
  },
  "include": ["src"],
  "exclude": [
    "src/__tests__",
    "**/*.test.ts",
    "**/__mocks__/**",
    "**/*.mock.ts",
    "vitest.config.ts"
  ]
}
