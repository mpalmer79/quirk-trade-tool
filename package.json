{
  "name": "quirk-trade-tool",
  "private": true,
  "version": "0.1.0",
  "workspaces": ["frontend", "orchestrator"],
  "scripts": {
    "dev:frontend": "pnpm --filter frontend dev",
    "dev:orchestrator": "pnpm --filter orchestrator dev",
    "dev": "run-p dev:*",

    // --- add these for GitHub Pages ---
    "build:pages": "pnpm --filter frontend run build:pages",
    "export:pages": "pnpm --filter frontend run export"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "packageManager": "pnpm@9.0.0"
}
