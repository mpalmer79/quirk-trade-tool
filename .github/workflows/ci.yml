name: CI/CD Pipeline - Frontend & Orchestrator

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  frontend-tests:
    runs-on: ubuntu-latest
    continue-on-error: true  # Temporary: Allow job to continue even if tests fail
    
    steps:
      # ... your existing steps
      
  build-frontend:
    runs-on: ubuntu-latest
    continue-on-error: true  # Temporary: Allow job to continue even if build fails
    
    steps:
      # ... your existing steps
      
  build-orchestrator:
    runs-on: ubuntu-latest
    continue-on-error: true  # Temporary: Allow job to continue even if build fails
    
    steps:
      # ... your existing steps
