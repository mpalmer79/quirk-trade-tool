# Keep one lockfile at the root for the whole workspace
shared-workspace-lockfile=true

# Make peer deps less brittle in CI; pnpm logs if something is off
strict-peer-dependencies=false
auto-install-peers=true

# Standard semver save prefix
save-prefix=^
